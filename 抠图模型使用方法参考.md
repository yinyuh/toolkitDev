在 Astro 中使用 `bria-rmbg-1.4.onnx` 进行背景移除，核心是通过 **ONNX Runtime Web** 在浏览器端加载并运行模型，结合 Canvas 进行图像处理。以下是完整的分步指南：

### 1. 项目准备
#### 1.1 安装依赖
在 Astro 项目中安装 ONNX Runtime Web：
```bash
npm install onnxruntime-web
```

#### 1.2 获取模型
从 [Hugging Face](https://huggingface.co/briaai/RMBG-1.4) 下载 `bria-rmbg-1.4.onnx` 模型文件，将其放置在 Astro 项目的 `public/models/` 目录下（确保可通过 `/models/bria-rmbg-1.4.onnx` 访问）。

---

### 2. 创建 Astro 客户端组件
由于需要在浏览器端操作 DOM 和 Canvas，需创建一个**客户端组件**（使用 `client:load` 指令）。

#### 示例：`src/components/BackgroundRemover.astro`
```astro
---
// 组件前端逻辑（服务端不执行）
---

<div class="container">
  <input type="file" id="imageInput" accept="image/*" />
  <div class="preview">
    <canvas id="inputCanvas" width="512" height="512"></canvas>
    <canvas id="outputCanvas" width="512" height="512"></canvas>
  </div>
</div>

<script client:load>
  import * as ort from 'onnxruntime-web';

  // 初始化模型会话
  let session;
  async function initModel() {
    try {
      session = await ort.InferenceSession.create('/models/bria-rmbg-1.4.onnx', {
        executionProviders: ['wasm'] // 使用 WASM 后端
      });
      console.log('模型加载成功');
    } catch (err) {
      console.error('模型加载失败:', err);
    }
  }

  // 图像预处理：调整大小 + 归一化 + 转 NCHW 格式
  function preprocessImage(canvas) {
    const ctx = canvas.getContext('2d');
    const imgData = ctx.getImageData(0, 0, 512, 512);
    const data = imgData.data;

    // 1. 归一化 (0-255 -> 0-1) 并提取 RGB 通道
    const floatData = new Float32Array(3 * 512 * 512);
    for (let i = 0; i < 512 * 512; i++) {
      floatData[i] = data[i * 4] / 255.0;     // R
      floatData[i + 512 * 512] = data[i * 4 + 1] / 255.0; // G
      floatData[i + 2 * 512 * 512] = data[i * 4 + 2] / 255.0; // B
    }

    // 2. 构造 NCHW 格式输入 (batch=1, channels=3, height=512, width=512)
    const inputTensor = new ort.Tensor('float32', floatData, [1, 3, 512, 512]);
    return inputTensor;
  }

  // 图像后处理：将模型输出转为透明背景图
  function postprocessAndRender(outputTensor, inputCanvas, outputCanvas) {
    const outputData = outputTensor.data;
    const ctx = outputCanvas.getContext('2d');
    const inputCtx = inputCanvas.getContext('2d');
    const inputImgData = inputCtx.getImageData(0, 0, 512, 512);
    const outputImgData = ctx.createImageData(512, 512);

    // 遍历像素，根据 mask 设置透明度
    for (let i = 0; i < 512 * 512; i++) {
      const maskValue = outputData[i]; // 模型输出是 1x1x512x512 的概率图
      outputImgData.data[i * 4] = inputImgData.data[i * 4];     // R
      outputImgData.data[i * 4 + 1] = inputImgData.data[i * 4 + 1]; // G
      outputImgData.data[i * 4 + 2] = inputImgData.data[i * 4 + 2]; // B
      outputImgData.data[i * 4 + 3] = maskValue * 255; // 透明度：mask 概率 * 255
    }

    ctx.putImageData(outputImgData, 0, 0);
  }

  // 处理图片上传
  async function handleImageUpload(event) {
    const file = event.target.files[0];
    if (!file || !session) return;

    // 1. 读取并绘制图片到输入 Canvas
    const img = new Image();
    img.onload = async () => {
      const inputCanvas = document.getElementById('inputCanvas');
      const ctx = inputCanvas.getContext('2d');
      ctx.drawImage(img, 0, 0, 512, 512); // 调整为模型输入尺寸

      // 2. 预处理
      const inputTensor = preprocessImage(inputCanvas);

      // 3. 模型推理
      const outputs = await session.run({ input: inputTensor }); // 注意：输入名需与模型一致（通常是 "input" 或 "input0"）
      const outputTensor = outputs[Object.keys(outputs)[0]]; // 获取第一个输出

      // 4. 后处理并渲染
      const outputCanvas = document.getElementById('outputCanvas');
      postprocessAndRender(outputTensor, inputCanvas, outputCanvas);
    };
    img.src = URL.createObjectURL(file);
  }

  // 页面加载完成后初始化
  document.addEventListener('DOMContentLoaded', () => {
    initModel();
    document.getElementById('imageInput').addEventListener('change', handleImageUpload);
  });
</script>

<style>
  .container { padding: 20px; }
  .preview { display: flex; gap: 20px; margin-top: 20px; }
  canvas { border: 1px solid #ddd; max-width: 512px; }
</style>
```

---

### 3. 在页面中使用组件
在任意 Astro 页面（如 `src/pages/index.astro`）中引入并使用组件：
```astro
---
import BackgroundRemover from '../components/BackgroundRemover.astro';
---

<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <title>背景移除工具</title>
  </head>
  <body>
    <h1>Astro + BRIA RMBG-1.4 背景移除</h1>
    <BackgroundRemover />
  </body>
</html>
```

---

### 关键注意事项
1. **模型输入名**：确保 `session.run({ input: inputTensor })` 中的输入名（如 `input`、`input0`）与 ONNX 模型的输入节点名一致。可通过 [Netron](https://netron.app/) 打开模型文件查看节点名。
2. **性能优化**：`bria-rmbg-1.4.onnx` 模型较大（约 170MB），首次加载会较慢，可考虑添加加载动画。
3. **WASM 后端**：ONNX Runtime Web 默认使用 WASM 后端，如需更高性能，可尝试 WebGL 后端（需模型支持）。
