---
interface Props {
  icon: any;
  title: string;
  description: string;
}

const { icon: Icon, title, description } = Astro.props;
---

<section
  class="group bg-div-theme relative flex flex-col items-center justify-center overflow-hidden rounded-2xl p-6 shadow-lg transition-transform hover:scale-102 hover:shadow-xl drop-shadow-[4px_4px_0_#7836cf]"
>
  <!-- Glow efecto -->
  <div
    class="absolute inset-0 -z-10 opacity-0 blur-2xl transition duration-500 group-hover:opacity-100"
  >
  </div>

  <!-- Icono -->
  <div
    class="mb-4 drop-shadow-[2px_2px_0_#7836cf] text-4xl text-[#02ffee]"
  >
    {Icon && <Icon />}
  </div>

  <!-- Título -->
  <h3 class="mb-2 text-xl font-bold text-div-theme drop-shadow-[2px_2px_0_#7836cf]">{title}</h3>

  <!-- Descripción -->
  <p class="text-center text-theme-secondary">{description}</p>
</section>